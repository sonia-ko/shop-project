import Product from "../interfaces/product";

export const getUniqueItems = (
  products: Product[],
  items: string | string[]
) => {
  // const set = new Set<string>();
  // switch (typeof items) {
  //   case 'string':
  // }
  // state.visibleProducts.forEach((product) =>
  //   product.categories.forEach((farm) => categories.add(farm))
  // );
  // state.farms = Array.from(farms.values());
};
